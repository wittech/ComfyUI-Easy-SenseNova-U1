## Design Context

### Users

本项目面向在 ComfyUI 中本地运行 SenseNova-U1 的创作者和技术用户。他们需要在不离开节点画布的情况下完成参考图上传、可视化定位、提示词编辑与生成，并能可靠复用和分享工作流。

### Brand Personality

专业、直接、可靠。界面应让用户感到操作可控、状态清楚，重点服务图像生成任务而不是表现独立绘图应用的品牌感。

### Aesthetic Direction

遵循 ComfyUI 原生深色界面、紧凑控件和节点化布局，同时兼容主题变量。避免与宿主界面割裂的重装饰、独立应用式导航和无意义动效。

### Design Principles

1. 上传即编辑：输入图片后立即看到画布，不依赖预运行或隐藏同步步骤。
2. 一图一输出：所有视觉标记在节点内合成成单张 `IMAGE`，不引入额外 bbox、mask 或串联参数。
3. 可逆且可复用：标注保持可选择、移动、缩放、撤销/重做，并随工作流保存。
4. 精确优先：为框选、圆圈、箭头、自由笔、文字和画布缩放提供明确控件及键盘快捷键。
5. 安全保真：标注绑定来源图片和尺寸；切换图片时不复用旧标注，编辑手柄也不进入最终像素。
